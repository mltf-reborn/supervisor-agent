apiVersion: v1
kind: Service
metadata:
  name: supervisor-agent
  labels:
    app: supervisor-agent
spec:
  type: NodePort
  selector:
    app: supervisor-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
